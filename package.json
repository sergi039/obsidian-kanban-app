{
  "name": "obsidian-kanban-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "test:watch": "pnpm -r --parallel run test:watch",
    "reminders:agent": "node scripts/reminder-agent.mjs --once",
    "reminders:agent:loop": "node scripts/reminder-agent.mjs --loop",
    "reminders:macos:install": "bash scripts/install-macos-reminders.sh"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
